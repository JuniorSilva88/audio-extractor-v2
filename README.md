# Audio Extractor v2

Sistema para extrair áudio de vídeos via link ou upload.

## Tecnologias
- Python (backend)
- HTML, CSS, JS (frontend)

## Como rodar
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r backend/requirements.txt
python backend/app.py
